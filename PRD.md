# PRD — เว็บแนะนำการท่องเที่ยวญี่ปุ่นแบบ Interactive (Travel Japan)

> **สถานะ:** `ready-for-agent`
> **ประเภท:** Feature (โปรเจกต์ใหม่ — greenfield)
> **หมายเหตุ:** ไม่มี issue tracker / git remote ในสภาพแวดล้อมนี้ จึง publish PRD เป็นไฟล์นี้แทน (fallback ตาม /to-prd)

---

## Problem Statement

ผู้ใช้ (นักท่องเที่ยวไทยที่สนใจญี่ปุ่นสายเกม/อนิเมะ/ของกิน) อยากรู้ว่า "ไปญี่ปุ่นแล้วควรไปที่ไหน ซื้ออะไร ราคาประมาณเท่าไหร่เป็นเงินบาท" แต่ข้อมูลเหล่านี้กระจัดกระจายอยู่หลายเว็บ เป็นภาษาญี่ปุ่น/อังกฤษ ราคาเป็นเยน ต้องแปลงเอง และไม่มีที่รวมที่บอกได้ว่าร้าน/สถานที่นั้นอยู่ตรงไหนบนแผนที่ ทำให้วางแผนเที่ยวยากและเสียเวลา

## Solution

เว็บ static แบบ interactive หน้าเดียว ที่รวบรวมร้าน/สถานที่ยอดนิยมในญี่ปุ่นจัดเป็น 5 หมวด แสดงเป็นกริดการ์ดพร้อมรูปภาพ ปักหมุดตำแหน่งจริงบนแผนที่ และระบุราคาสินค้า/เมนูตัวอย่างทั้งเป็นเยน (¥) และบาท (฿) โดยคำนวณจากอัตราแลกเปลี่ยนสดตอนเปิดเว็บ ทุก entry มีลิงก์อ้างอิงแหล่งข้อมูลที่น่าเชื่อถือ ผู้ใช้สามารถกรองตามหมวด/เมือง ค้นหา เรียงตามราคา บันทึกรายการที่ถูกใจ และดูรายละเอียดพร้อมแกลเลอรีรูปในโมดัลได้

## User Stories

1. ในฐานะนักท่องเที่ยว ฉันอยากเห็นภาพรวมร้าน/สถานที่ทั้งหมดเป็นกริดการ์ดพร้อมรูป เพื่อจะได้กวาดตาเลือกที่น่าสนใจได้เร็ว
2. ในฐานะนักท่องเที่ยว ฉันอยากกรองรายการตามหมวด (ท่องเที่ยว / เกม / คอสเพลย์ / เครื่องใช้ไฟฟ้า / อาหาร) เพื่อดูเฉพาะสิ่งที่สนใจ
3. ในฐานะนักท่องเที่ยว ฉันอยากกรองรายการตามเมือง (โตเกียว / โอซาก้า / เกียวโต) เพื่อวางแผนตามจังหวัดที่จะไป
4. ในฐานะนักท่องเที่ยว ฉันอยากพิมพ์คำค้นหา (ชื่อร้าน/สถานที่/สินค้า) เพื่อหาสิ่งที่ต้องการได้ตรงจุด
5. ในฐานะนักท่องเที่ยว ฉันอยากเรียงรายการตามราคา (ถูก→แพง และ แพง→ถูก) เพื่อเทียบความคุ้มค่า
6. ในฐานะนักท่องเที่ยว ฉันอยากเห็นตำแหน่งของทุกร้าน/สถานที่ปักหมุดบนแผนที่อินเตอร์แอคทีฟ เพื่อรู้ว่าแต่ละที่อยู่ตรงไหนและอยู่ใกล้กันไหม
7. ในฐานะนักท่องเที่ยว ฉันอยากคลิกหมุดบนแผนที่แล้วเห็น popup ชื่อ+รูป+ราคาย่อ เพื่อดูข้อมูลเร็วโดยไม่ต้องเปิดโมดัล
8. ในฐานะนักท่องเที่ยว ฉันอยากคลิกการ์ดเพื่อเปิดโมดัลรายละเอียด เพื่อดูข้อมูลครบ (คำอธิบาย, สินค้า/เมนูตัวอย่าง, ที่อยู่, แผนที่, แหล่งอ้างอิง)
9. ในฐานะนักท่องเที่ยว ฉันอยากเห็นราคาสินค้า/เมนูทั้งเป็นเยนและบาท เพื่อไม่ต้องแปลงค่าเงินเอง
10. ในฐานะนักท่องเที่ยว ฉันอยากเห็นว่าอัตราแลกเปลี่ยนที่ใช้คำนวณเป็นเรต ณ วันที่ไหน เพื่อรู้ว่าราคาบาทอัปเดตแค่ไหน
11. ในฐานะนักท่องเที่ยว ฉันอยากให้เว็บยังแสดงราคาบาทได้แม้ดึงเรตสดไม่สำเร็จ (ใช้เรตสำรอง) เพื่อไม่ให้หน้าเว็บพัง
12. ในฐานะนักท่องเที่ยว ฉันอยากเห็นชื่อร้าน/สถานที่ทั้งภาษาไทยและภาษาญี่ปุ่น/โรมาจิ เพื่อเอาไปถามทางหรือค้นหาตอนอยู่ญี่ปุ่นจริง
13. ในฐานะนักท่องเที่ยว ฉันอยากดูแกลเลอรีรูปหลายรูปของร้าน/สถานที่ในโมดัล เพื่อเห็นบรรยากาศก่อนตัดสินใจไป
14. ในฐานะนักท่องเที่ยว ฉันอยากกดปุ่ม "ถูกใจ/บุ๊กมาร์ก" ที่ร้าน/สถานที่ เพื่อรวบรวมที่ที่อยากไป
15. ในฐานะนักท่องเที่ยว ฉันอยากให้รายการที่บุ๊กมาร์กไว้ยังอยู่เมื่อกลับมาเปิดเว็บใหม่ (เก็บใน localStorage) เพื่อไม่ต้องเลือกซ้ำ
16. ในฐานะนักท่องเที่ยว ฉันอยากกรองดูเฉพาะรายการที่บุ๊กมาร์กไว้ เพื่อดูชอร์ตลิสต์ของตัวเอง
17. ในฐานะนักท่องเที่ยว ฉันอยากคลิกลิงก์แหล่งอ้างอิงของแต่ละ entry เพื่อตรวจสอบข้อมูล/ราคาจากต้นทาง
18. ในฐานะนักท่องเที่ยว ฉันอยากเห็นหน้ารวมเครดิต/แหล่งอ้างอิงและที่มาของรูปภาพ เพื่อความน่าเชื่อถือและให้เครดิตตามลิขสิทธิ์
19. ในฐานะนักท่องเที่ยวที่ใช้มือถือ ฉันอยากให้เว็บแสดงผลได้ดีบนจอเล็ก เพื่อเปิดดูระหว่างเดินทาง
20. ในฐานะผู้ใช้ ฉันอยากให้การเปลี่ยนหน้า/เปิดโมดัล/กรองรายการมีแอนิเมชันนุ่มนวล เพื่อประสบการณ์ที่ลื่นไหล
21. ในฐานะผู้ใช้ ฉันอยากเห็นสถานะ "กำลังโหลด" และข้อความที่เข้าใจง่ายเมื่อไม่มีรายการตรงกับฟิลเตอร์ เพื่อไม่สับสนว่าเว็บพังหรือไม่
22. ในฐานะผู้ใช้ ฉันอยากให้กริดการ์ดและแผนที่ sync กัน (กรอง/ค้นหาแล้วทั้งสองอัปเดตพร้อมกัน) เพื่อมุมมองที่สอดคล้อง
23. ในฐานะนักท่องเที่ยว ฉันอยากเห็นป้ายบอกหมวดและเมืองบนการ์ด เพื่อรู้บริบทของแต่ละรายการโดยไม่ต้องเปิดดู
24. ในฐานะนักท่องเที่ยวสายเกม ฉันอยากรู้ว่าร้านเกมขายของมือ 1 หรือมือ 2 เพื่อเลือกตามงบและความต้องการ

## Implementation Decisions

### โครงสร้างไฟล์ (multi-file, static, ไม่มี bundler)
- `index.html` — โครงหน้า + โหลด Leaflet จาก CDN + `<script type="module" src="app.js">`
- `style.css` — สไตล์ทั้งหมด (ธีมสะอาด มินิมอล สว่าง, responsive)
- `app.js` — ES module: orchestration, DOM rendering, Leaflet, `fetch`, localStorage (ชั้น side-effect)
- `logic.js` — **ES module ของ pure functions** (ชั้น logic ล้วน — เป็น seam หลักสำหรับเทสต์); ถูก import โดยทั้ง `app.js` และ Vitest
- `data.json` — dataset ร้าน/สถานที่ (ฝังข้อมูลที่ research มา)
- `package.json` — สำหรับ Vitest (dev dependency เท่านั้น; ตัวเว็บไม่มี build step ใช้งานจริง)
- `README.md` — วิธีรัน (ต้องผ่าน local web server) + วิธีรันเทสต์

### ⚠️ ต้องรันผ่าน local web server
`app.js` ใช้ `fetch('data.json')` ซึ่งติด CORS ถ้าเปิดด้วย `file://` (double-click). README ต้องบอกให้รันผ่าน `python -m http.server` หรือ VS Code Live Server. (เป็นข้อยกเว้นจากธรรมเนียม "double-click เปิดได้เลย" ของรีโป — แลกกับโครงสร้าง multi-file + data.json ที่ผู้ใช้เลือก)

### Data model — หน่วยหลักคือ "ร้าน/สถานที่" (Entry)
แต่ละ entry ใน `data.json` มีรูปแบบ (โครงเชิงตรรกะ ไม่ผูกชื่อฟิลด์ตายตัวกับโค้ด):
- `id`, `nameTh`, `nameJa`, `nameRomaji`
- `category` — หนึ่งใน: `sightseeing | games | cosplay | electronics | food`
- `city` — เช่น `tokyo | osaka | kyoto`
- `description` (ไทย)
- `lat`, `lng` — พิกัดสำหรับปักหมุด Leaflet
- `address` (ไทย + ญี่ปุ่น)
- `images[]` — URL (Wikimedia Commons ถ้ามี) แต่ละรูปมี `url` + `credit`/`license`; ถ้าไม่มีให้ fallback เป็น placeholder
- `products[]` — สินค้า/เมนูตัวอย่าง แต่ละชิ้นมี `nameTh`, `priceJpy`, (optional) `condition` สำหรับเกม (`new`/`used`)
- `sourceUrl` — ลิงก์แหล่งอ้างอิงต่อ entry
- ราคา **เก็บเป็น JPY เสมอ**; บาทคำนวณตอน runtime

### ราคา / อัตราแลกเปลี่ยน — "เก็บ JPY → คำนวณบาทสด"
- ตอนโหลด: `fetch` เรต JPY→THB จาก free API (เช่น exchangerate-style endpoint ที่ไม่ต้องใช้ key)
- คำนวณบาท = `priceJpy * rate` ด้วย `convertJpyToThb(jpy, rate)` (pure function)
- แสดงทั้ง `¥X,XXX` และ `฿X,XXX` ในการ์ดสรุปและในโมดัล
- แสดงป้าย "เรต ณ วันที่ …" จาก response
- **Fallback:** ถ้า fetch ล้มเหลว ใช้เรตสำรองที่ฝังไว้ในโค้ด (พร้อมหมายเหตุว่าเป็นเรตโดยประมาณ) — เว็บต้องไม่พัง

### แผนที่ — Leaflet + OpenStreetMap
- โหลด Leaflet จาก CDN, ไม่ต้องใช้ API key
- ปักหมุดทุก entry ที่ผ่านฟิลเตอร์ปัจจุบัน; หมุดมี popup (ชื่อ + รูป + ราคาเริ่มต้น + ปุ่มเปิดโมดัล)
- แผนที่ sync กับฟิลเตอร์/ค้นหา (อัปเดตหมุดเมื่อ view model เปลี่ยน)

### UI/UX — หน้าเดียว: hero → filter → กริด ↔ แผนที่ → โมดัล
- แถบฟิลเตอร์: เลือกหมวด, เลือกเมือง, ช่องค้นหา, ปุ่มเรียงราคา, toggle "เฉพาะที่บุ๊กมาร์ก"
- ธีม: สะอาด มินิมอล สว่าง เน้นรูปภาพ; แอนิเมชัน fade/slide ตอนกรอง, hover การ์ด, modal เปิด/ปิดนุ่มนวล
- ภาษา: ไทยเป็นหลัก + ชื่อญี่ปุ่น/โรมาจิกำกับ
- โมดัล: แกลเลอรีรูปหลายรูป + รายละเอียด + แผนที่ย่อ + ลิงก์อ้างอิง
- บุ๊กมาร์ก: เก็บ id ใน `localStorage`; toggle ได้จากการ์ดและโมดัล

### Seam สำหรับเทสต์ (seam เดียว ที่จุดสูงสุด)
แยก pure logic ทั้งหมดไว้ใน `logic.js` รับ input → คืน output โดยไม่แตะ DOM/network/Leaflet:
- `filterEntries(entries, { category, city, query, bookmarkedOnly, bookmarkedIds })` → entries ที่ผ่านเงื่อนไข
- `sortByPrice(entries, direction)` → entries เรียงตามราคาเริ่มต้น (ราคา min ของ products)
- `convertJpyToThb(jpy, rate)` → บาท (ปัดเศษตามที่กำหนด)
- `getStartingPriceJpy(entry)` → ราคาต่ำสุดในสินค้าของ entry (ใช้เรียง/แสดงสรุป)
- (อาจมี) `searchMatches(entry, query)` — ตรรกะ matching คำค้น
`app.js` เรียกใช้ฟังก์ชันเหล่านี้แล้วเอาผลไป render; ชั้น side-effect ไม่ถูกเทสต์ตรงๆ

## Testing Decisions

- **เครื่องมือ:** Vitest (npm) — ผู้ใช้เลือกเต็มรูปแบบ; เพิ่ม `package.json` + `npm test`
- **เทสต์เฉพาะพฤติกรรมภายนอกของ `logic.js`** (input → output) ไม่เทสต์ implementation detail และไม่เทสต์ DOM/Leaflet/fetch
- **กรณีที่ต้องครอบ:**
  - `filterEntries`: กรองหมวดเดียว, กรองเมือง, ค้นหาตรง/ไม่ตรง, bookmarkedOnly, การ combine หลายเงื่อนไข, ผลลัพธ์ว่าง
  - `sortByPrice`: ถูก→แพง, แพง→ถูก, entry ที่ไม่มี products, ราคาเท่ากัน
  - `convertJpyToThb`: คำนวณถูกต้อง, ปัดเศษ, rate = fallback, jpy = 0
  - `getStartingPriceJpy`: หลาย products, product เดียว, ไม่มี product
- **Prior art:** รีโปยังไม่มีเทสต์มาก่อน — ชุดนี้เป็นชุดแรก จึงตั้งแพทเทิร์น: หนึ่งไฟล์เทสต์ต่อ logic module (`logic.test.js`), จัดกลุ่มด้วย `describe` ตามชื่อฟังก์ชัน, เคสละ `it` พร้อมข้อมูลตัวอย่างเล็กๆ inline

## Out of Scope

- Backend / ฐานข้อมูล / ระบบ user account — เป็น static ล้วน
- การแก้ไขข้อมูลผ่าน UI หรือ CMS — ข้อมูลแก้ที่ `data.json` โดยตรง
- เรตแลกเปลี่ยนแบบเรียลไทม์ต่อเนื่อง / ตัวแปลงค่าเงินที่ผู้ใช้กรอกเอง — v1 ดึงเรตครั้งเดียวตอนโหลด
- Multi-language UI / สลับ TH-EN — v1 ไทยเป็นหลัก
- การจอง/ซื้อ/ลิงก์ affiliate
- จำนวน entry มาก/ครบทุกเมือง — v1 จำกัด ~15 entry (3/หมวด × 5 หมวด) กระจายเมืองใหญ่ คุณภาพมาก่อนปริมาณ
- เทสต์ DOM/E2E (เช่น Playwright) — เทสต์เฉพาะ pure logic
- การ deploy ขึ้นโฮสติ้ง

## Further Notes

- **Research จำเป็นก่อนเขียน data.json:** ต้องค้นจากแหล่งน่าเชื่อถือเพื่อได้ชื่อร้าน/สถานที่จริง, พิกัด lat/lng, ราคาสินค้า/เมนูโดยประมาณ (JPY), รูปจาก Wikimedia Commons (พร้อม license/credit), และ sourceUrl ต่อ entry — ทำผ่าน WebSearch/WebFetch
- ถ้าหารูป Commons ไม่ได้ ให้ใช้ placeholder (gradient/icon) อย่างสุภาพ — ห้ามฮอตลิงก์รูปลิขสิทธิ์
- ราคาเป็น "โดยประมาณ" ควรมีหมายเหตุกำกับว่าอาจเปลี่ยนแปลง
- โครงสร้างเผื่อขยายในอนาคต (เพิ่มเมือง/หมวด/entry) โดยแค่เติมใน `data.json`
- เมื่อจะ implement: เริ่มจาก research → เขียน `data.json` → `logic.js` + เทสต์ → `app.js` + UI → README

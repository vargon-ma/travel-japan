# PRD — ขยาย Dataset เป็น ~50 ร้าน ครบ 8 หมวด + รูปจริง/ภาพประกอบ (Travel Japan)

> **สถานะ:** `ready-for-agent`
> **ประเภท:** Feature (ขยายเนื้อหา + schema เล็กน้อย + validator)
> **หมายเหตุ:** ไม่มี issue tracker / git remote ในสภาพแวดล้อมนี้ จึง publish PRD เป็นไฟล์นี้ (fallback ตาม /to-prd) — จะแตกเป็น `ISSUES/09`–`ISSUES/13`
> **อ้างอิง:** `PRD.md` (โปรเจกต์หลัก), `CONTEXT.md` (glossary), `docs/adr/0001` (image sourcing), `docs/adr/0002` (taxonomy/scale), `docs/adr/0003` (illustrative images)

---

## Problem Statement

จากมุมผู้ใช้ (นักท่องเที่ยวไทย): เปิดเว็บมาแล้วรู้สึกว่า **สถานที่ยังน้อยและไม่ครบ** — บางหมวดโล่ง (หมวด "ช้อปปิ้ง" ว่างเปล่า, "คาเฟ่" มีร้านเดียว) ทำให้กดชิปหมวดแล้วเจอ empty-state ดูเหมือนเว็บยังไม่เสร็จ นอกจากนี้ **การ์ดส่วนใหญ่เป็นอิโมจิ placeholder ไม่ใช่รูปจริง** (จริง ๆ คือ 22 จาก 28 ร้านยังไม่มีรูป) ทำให้เว็บดู "เงียบ/ราคาถูก/ไม่น่าเชื่อถือ" และร้านที่เพิ่งเพิ่มบางร้านก็ข้อมูลบาง (ไม่มีวิธีเดินทาง/เวลาทำการ/ทิป) เทียบกับร้าน landmark ที่ข้อมูลแน่น ทำให้คุณภาพไม่สม่ำเสมอ

## Solution

จากมุมผู้ใช้: ขยายเนื้อหาเป็น **~49 ร้าน ครบทั้ง 8 หมวด** ใน 3 เมืองเดิม (โตเกียว/โอซาก้า/เกียวโต) โดย **เติมหมวดที่โล่งให้มีตัวแทนคุณภาพ** (โดยเฉพาะช้อปปิ้งจาก 0→5, คาเฟ่ 1→4, ของสะสมอนิเมะ 2→5) และ **ขยายหมวดยอดนิยม** (อาหาร/เกม/ท่องเที่ยว) ให้แน่นขึ้นตามความสนใจจริงของคนไทย ทุกร้านใหม่ผ่านเกณฑ์คุณภาพขั้นต่ำเดียวกัน (มีวิธีเดินทาง เวลาทำการ แท็ก ทิป ราคาตัวอย่าง) และ **แต่ละร้านมีรูปที่ดูมีชีวิต** — รูปจริงเมื่อหาได้ หรือ "ภาพประกอบ" ที่กำกับชัดว่าไม่ใช่ภาพร้านจริงเมื่อหารูปเสรีไม่ได้ พร้อมรีเฟรช Editor's Pick ให้ครอบคลุมทุกแนวและกระจายเมือง

## User Stories

1. ในฐานะนักท่องเที่ยว ฉันอยากให้ทุกหมวด (รวมช้อปปิ้ง/คาเฟ่) มีร้านให้ดูอย่างน้อยหลายร้าน เพื่อไม่กดหมวดแล้วเจอหน้าว่าง
2. ในฐานะนักช้อป ฉันอยากเห็นหมวด "ช้อปปิ้ง" ที่มีดองกี้/ย่านช้อป/ห้างไลฟ์สไตล์ เพื่อวางแผนซื้อของฝากและช้อปทั่วไป
3. ในฐานะสายคาเฟ่ ฉันอยากเห็นคาเฟ่ที่มีเอกลักษณ์หลายร้าน เพื่อเลือกนั่งพักระหว่างเที่ยว
4. ในฐานะโอตาคุ ฉันอยากเห็นร้านของสะสมอนิเมะ/ฟิกเกอร์/การ์ดมากขึ้น เพื่อตามล่าของที่อยากได้
5. ในฐานะสายกิน ฉันอยากเห็นร้านอาหารหลากหลายขึ้น (ซูชิ/ปลาไหล/ราเมง/อื่น ๆ) เพื่อมีตัวเลือกมื้ออาหารครบ
6. ในฐานะนักท่องเที่ยว ฉันอยากเห็นสถานที่ท่องเที่ยว/แลนด์มาร์กเพิ่ม เพื่อวางแผนจุดเที่ยวหลักได้มากขึ้น
7. ในฐานะนักท่องเที่ยว ฉันอยากให้ทุกร้านมีรูปที่ดูจริงจัง (ไม่ใช่อิโมจิเปล่า) เพื่อให้เว็บดูน่าเชื่อถือและช่วยตัดสินใจ
8. ในฐานะผู้ใช้ที่ใส่ใจความถูกต้อง ฉันอยากรู้เมื่อรูปหนึ่งเป็น "ภาพประกอบ" ไม่ใช่ภาพร้านจริง เพื่อไม่เข้าใจผิดว่าร้านหน้าตาแบบนั้น
9. ในฐานะนักท่องเที่ยว ฉันอยากให้ทุกร้านบอกวิธีเดินทาง (สถานีรถไฟ) เพื่อไปถึงได้จริง
10. ในฐานะนักท่องเที่ยว ฉันอยากให้ทุกร้านบอกเวลาทำการโดยประมาณ เพื่อไม่ไปแล้วเจอปิด
11. ในฐานะนักท่องเที่ยว ฉันอยากได้ทิปเชิงปฏิบัติของแต่ละร้าน (เช่น ไปเช้าคนน้อย) เพื่อเที่ยวได้ราบรื่นขึ้น
12. ในฐานะนักท่องเที่ยว ฉันอยากเห็นแท็กจุดเด่นของแต่ละร้าน เพื่อกวาดตาเลือกตามอารมณ์/คุณลักษณะ
13. ในฐานะนักท่องเที่ยว ฉันอยากให้ราคาตัวอย่างของทุกร้านมีอย่างน้อยสองรายการ เพื่อพอเห็นช่วงราคา
14. ในฐานะนักท่องเที่ยว ฉันอยากค้นหาร้านใหม่ ๆ ด้วยชื่อ/สินค้า/แท็กได้เหมือนร้านเดิม เพื่อหาของที่อยากได้
15. ในฐานะนักท่องเที่ยว ฉันอยากให้ร้านใหม่ทุกร้านปักหมุดถูกตำแหน่งบนแผนที่ เพื่อดูว่าอยู่ใกล้กันไหม
16. ในฐานะนักท่องเที่ยว ฉันอยากเปิดโมดัลร้านใหม่แล้วเห็นข้อมูลครบเท่าร้านเดิม (แกลเลอรี/fact grid/ทิป/แหล่งอ้างอิง) เพื่อคุณภาพที่สม่ำเสมอ
17. ในฐานะนักท่องเที่ยวครั้งแรก ฉันอยากให้ Featured strip โชว์ตัวแทนเด็ดของทุกแนว (ไม่ใช่แค่แลนด์มาร์ก/อาหาร) เพื่อเห็นความหลากหลายทันที
18. ในฐานะนักท่องเที่ยว ฉันอยากให้ Editor's Pick แต่ละอันมีเหตุผลสั้น ๆ ที่จับต้องได้ เพื่อเข้าใจว่าทำไมถึงแนะนำ
19. ในฐานะผู้ใช้ที่ใส่ใจลิขสิทธิ์ ฉันอยากให้ทุกรูป (จริงหรือประกอบ) มีเครดิต/แหล่งที่มา/สัญญาอนุญาต เพื่อความถูกต้อง
20. ในฐานะผู้ใช้ ฉันอยากให้ราคา/เวลาทำการมีป้ายกำกับว่า "โดยประมาณ" เพื่อรู้ว่าเป็นค่าอ้างอิง ไม่ใช่ข้อมูลเรียลไทม์
21. ในฐานะนักท่องเที่ยว ฉันอยากมั่นใจว่าไม่มีร้านไหน "ข้อมูลบาง/ผี" หลุดออกมา เพื่อคุณภาพทั้งเว็บ (ผู้ใช้ไม่เห็นตรง ๆ แต่ได้ประโยชน์จาก validator)
22. ในฐานะผู้ดูแลข้อมูล ฉันอยากให้ `npm test` แดงทันทีถ้าเพิ่มร้านที่ไม่ผ่านเกณฑ์คุณภาพ เพื่อกันข้อมูลบกพร่องตั้งแต่ก่อนขึ้นเว็บ
23. ในฐานะผู้ดูแลข้อมูล ฉันอยากให้ validator เตือนเมื่อ `category`/`city` สะกดผิดหรือหลุด closed set เพื่อกันฟิลเตอร์พัง
24. ในฐานะผู้ดูแลข้อมูล ฉันอยากให้ validator เตือนเมื่อรูปไม่มี credit/license เพื่อบังคับตามนโยบายลิขสิทธิ์
25. ในฐานะนักช้อป ฉันอยากให้การจัดหมวดของร้านที่ขายปนกัน (เช่นดองกี้) สอดคล้องกันทั้งเว็บ เพื่อฟิลเตอร์แล้วเจอในหมวดที่คาด

## Implementation Decisions

**ขอบเขตเมือง**
- คง `City` เป็น closed set 3 ค่าเดิม (`tokyo | osaka | kyoto`) ไม่ขยายเมืองในรอบนี้ — เมืองใหม่ (Nara/Yokohama/…) เป็น slice แยกในอนาคต

**เป้าหมายการกระจาย (~49 ร้าน)**
- ยึดความนิยมจริง + floor ขั้นต่ำ **3 ร้าน/หมวด** (ไม่มีหมวดใดต่ำกว่านี้ กัน empty-state)
- เป้าต่อหมวด (ปรับได้เล็กน้อยตอนลงมือ): food 12, games 7, sightseeing 7, electronics 5, anime-goods 5, shopping 5, cosplay 4, cafe 4
- กระจายเมืองให้สมเหตุผล ไม่กระจุกโตเกียวเกินจำเป็น

**นิยามหมวด `shopping` (แก้ช่องว่างจัดหมวด)**
- `shopping` = ช้อปทั่วไป/ไลฟ์สไตล์/ของฝาก ที่ **ไม่ใช่** เครื่องใช้ไฟฟ้า (`electronics`) และ **ไม่ใช่** ของโอตาคุ (`anime-goods`)
- เกณฑ์ตัดสินตอนคาบเกี่ยว: **"ร้านนี้ขายอะไรเป็นหลัก"** — ดองกี้ที่ขายปนกัน → `shopping`
- ตั้งต้น: Don Quijote, Shibuya 109, Tokyu Hands/Loft, Shinsaibashi-suji (โอซาก้า), Nakamise (อาซากุสะ)
- บันทึกใน `CONTEXT.md` (ส่วน Category) แล้ว

**Definition of Done ต่อร้าน (บังคับสำหรับร้านใหม่ + ย้อนเก็บร้านเดิม)**
- ฟิลด์บังคับตาม schema ครบ 100% + `lat/lng` แม่นระดับปักหมุดถูกตึก
- ยกให้เป็น de-facto บังคับเพิ่ม: `station`, `hours`, `tags[]` ≥ 2, `tips[]` ≥ 1, `products[]` ≥ 2 (landmark ใส่ค่าเข้าชม/"ฟรี"), `googleMapsUrl`
- `bestTime` แนะนำแต่ไม่บังคับ
- ราคา/เวลา "โดยประมาณ" ต้องมีป้ายกำกับ; ห้ามแต่งเรตติ้ง/รีวิว (ไม่มีช่องอยู่แล้ว)

**Schema เพิ่มฟิลด์ `isIllustrative` (ตาม ADR-0003)**
- เพิ่ม `isIllustrative?: boolean` บนแต่ละรายการใน `images[]` — `true` = ภาพประกอบบริบท ไม่ใช่ภาพร้านจริง
- ทุกรูปยังต้องมี `source`/`credit`/`license` เสมอ (ADR-0001)
- ฝั่ง UI (side-effect layer) เรนเดอร์ป้าย "ภาพประกอบ" มุมรูปเมื่อ `isIllustrative === true` ทั้งในการ์ด/แกลเลอรีโมดัล
- ลำดับความพยายามเลือกรูปต่อร้าน: (1) รูปจริงเสรี → (2) ภาพประกอบเสรี + ธง → (3) placeholder ตามหมวด (fallback เดิม)

**Validator ใน `logic.js` (seam เดียว)**
- เพิ่ม pure function `validateEntry(entry)` คืน **array ของ error string** (ว่าง = ผ่าน) — ไม่ throw, ไม่แตะ DOM/network
- ตรวจ: ฟิลด์บังคับครบ; `category` ∈ closed set 8 ค่า; `city` ∈ closed set 3 ค่า; DoD (`station`, `hours`, `tags`≥2, `tips`≥1, `products`≥2); ทุก product มี `nameTh` + `priceJpy` เป็นตัวเลข ≥ 0; ทุก image มี `url` + `credit`/`source` + `license`; `lat`/`lng` เป็นตัวเลขในช่วงพิกัดญี่ปุ่นคร่าว ๆ; `id` ไม่ซ้ำ (เช็คระดับ dataset)
- อาจเสริม `validateDataset(entries)` (pure) สำหรับเช็ค id ซ้ำ/นับ floor ต่อหมวด — optional ถ้าช่วยให้ test อ่านง่าย
- สไตล์โค้ด: ตามแบบ `logic.js` เดิม (JSDoc ภาษาไทย, ป้องกัน input เพี้ยนแบบ `getPriceLevel`/`filterEntries`)

**Editor's Pick / Featured**
- ~8–10 ร้าน; อย่างน้อย 1 ต่อหมวดที่มีของเด็ดจริง; กระจายเมือง; ทุกอันมี `editorsPickReason` ที่จับต้องได้ (ไม่ลอย)
- คัดใหม่หลังของครบ (issue ท้ายสุด)

**การแตกงาน (→ `ISSUES/09`–`13`, แต่ละก้อนหยิบทำจบเอง; B–E พึ่ง A)**
- **09 (A) — Foundation:** ฟิลด์ `isIllustrative` + ป้าย UI + `validateEntry`/`validateDataset` ใน `logic.js` + test + อัปเดต `CLAUDE.md`/README ให้ตรง
- **10 (B) — หมวดที่โล่ง:** shopping (0→5), cafe (1→4), anime-goods (2→5) พร้อมรูป
- **11 (C) — หมวดยอดนิยม:** food (10→12), games (5→7), sightseeing (4→7), electronics (3→5), cosplay (3→4) พร้อมรูป
- **12 (D) — ย้อนอัปเกรดร้านเดิม:** เติม station/hours/tags/tips ที่ขาดให้ถึง DoD + ใส่รูป (จริง/ประกอบ) ให้ 22 ร้านที่ยังไม่มีรูป
- **13 (E) — คัด Editor's Pick ใหม่ 8–10** ให้ครบแนว + กระจายเมือง

## Testing Decisions

- **หลักการ:** ทดสอบเฉพาะพฤติกรรมภายนอก (input → output) ของ `logic.js` เท่านั้น ไม่ทดสอบ DOM/Leaflet/fetch/localStorage/รายละเอียดภายใน — ตามกฎโปรเจกต์เดิม
- **โมดูลที่ทดสอบ:** `logic.js` — ฟังก์ชันใหม่ `validateEntry` (และ `validateDataset` ถ้ามี)
- **Prior art:** `logic.test.js` จัดกลุ่มด้วย `describe` ต่อฟังก์ชัน (เช่น `getPriceLevel`, `wikiThumbUrl`, `filterEntries`) — เพิ่ม `describe('validateEntry')` ในสไตล์เดียวกัน: เคส entry สมบูรณ์→ไม่มี error, เคสขาดแต่ละ DoD field→มี error ตรงจุด, category/city นอก closed set→error, image ไม่มี license→error, product ราคาไม่ใช่ตัวเลข→error
- **Dataset guard test:** เพิ่มเทสต์ที่ `import` `data.json` แล้ววน `validateEntry` ทุก entry — คาดว่าได้ 0 error รวม (ถ้าใครเพิ่มร้านบกพร่อง เทสต์นี้แดงทันที) และเช็ค id ไม่ซ้ำ + floor ≥ 3 ต่อหมวด
- ไม่เพิ่ม E2E/DOM test; ป้าย "ภาพประกอบ" และ featured strip เป็น side-effect layer จึงไม่ unit-test ตรง ๆ

## Out of Scope

- **เมืองใหม่** (Nara/Yokohama/Hakone/…) — คง 3 เมือง
- **แชร์ / deep-link / Open Graph** — slice แยก (เคยคุยไว้ก่อนหน้า)
- **Trip planner / ฟิลเตอร์ฉลาด** ("เปิดตอนนี้" จาก hours, กรองตาม priceLevel/tags) — slice แยก
- **PWA/offline, backend/CMS, แก้ข้อมูลผ่าน UI** — ไม่ทำ (ข้อมูลแก้ตรงใน `data.json`)
- **การเปลี่ยนดีไซน์/perf เพิ่ม** — ทำไปแล้วในเฟสก่อน ไม่แตะรอบนี้ (นอกจากป้าย isIllustrative ที่จำเป็น)
- **JSON Schema ไฟล์แยก (เช่น ajv)** — ใช้ `validateEntry` เป็น pure function ใน seam เดิมพอ ไม่เพิ่ม dependency

## Further Notes

- คงคอนเทนต์/โค้ด/PRD เป็น **ภาษาไทย** ให้สอดคล้องทั้งโปรเจกต์
- ทุกร้านต้อง web-research แหล่งที่เชื่อถือได้ ใส่ `sourceUrl`; ราคา/เวลา label "โดยประมาณ"
- ข้อจำกัดรูป Wikimedia: ใช้ thumbnail ได้เฉพาะขนาดใน `WIKI_THUMB_SIZES` (250/500px) ผ่าน `wikiThumbUrl` เดิม — สำหรับแหล่งอื่น (Unsplash/Pexels) ใช้ URL ตรงตามที่แหล่งอนุญาต
- หลัง `/to-issues` ควรรักษา context hygiene: `/implement` แต่ละ issue ในเซสชันใหม่ ส่ง PRD นี้ + issue เดียวเข้าไป
